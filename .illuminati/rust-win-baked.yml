os: windows
image: windows-2022-rust
timeout: 600
steps:
  - "cargo --version && rustc --version && cargo new app && cd app && cargo build --release && echo BUILT_OK"
artifacts:
  - "app/target/release/app.exe"
