os: linux
image: ubuntu-24.04
timeout: 300
steps:
  - 'mkdir -p ~/.bcache && date +%s%N >> ~/.bcache/runs.txt'
  - 'cp ~/.bcache/runs.txt runs.txt; echo "cache lines: $(wc -l < runs.txt)"'
cache:
  - "~/.bcache"
artifacts:
  - "runs.txt"
